/** V1 placeholder: the weekly email digest is deferred. Table `digest_sends` exists; nothing is sent. */
export async function weeklyDigest() {
  console.log('[weeklyDigest] placeholder — not implemented in V1')
}
